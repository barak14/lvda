# Load lvda at boot so the persistent virtual-display card (/dev/dri/cardN)
# and its control device (/dev/lvda) are always present — the compositor can
# bind the card at startup and react to per-session connector hotplugs.
lvda
